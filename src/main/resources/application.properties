spring.application.name=capstone

# DB local connection
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA config
# Remove next line if Flyway will update DB:
spring.jpa.hibernate.ddl-auto=update 
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway config (telling Flyway I am using MySQL to avoid version conflict)
#spring.flyway.url=jdbc:mysql://localhost:3306/ada_capstone
#spring.flyway.user=root
#spring.flyway.password=Elialav
#spring.flyway.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.flyway.database-type=mysql

# Only if using Flyway:
#spring.jpa.hibernate.ddl-auto=validate


# Optional: Fix time zone issues
spring.datasource.hikari.connection-timezone=UTC

